version: "3"
services:
  tests:
    build:
      context: .
    volumes:
      - ".:/plugin"
  lint:
    image: "buildkite/plugin-linter"
    command: ["--id", "envato/bundle-update"]
    volumes:
      - ".:/plugin:ro"
